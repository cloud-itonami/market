;; shadow-cljs build for the market appview UI.
;; Mirrors orgs/cloud-itonami/app-itonami/shadow-cljs.edn:
;; CLJS + reagent on kotoba-ui.core + appkit.core (murakumo-studio構成).
{:deps {:aliases [:cljs]}
 :nrepl {:port 9632}

 :builds
 {:app
  {:target :browser
   :output-dir "appview/etzhayyim-wasm-market-mk7r3x9p/web/dist/js"
   :asset-path "js"
   :modules {:main {:init-fn cloud-itonami.market.desktop/init!}}}}}
